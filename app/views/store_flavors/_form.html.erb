<%= simple_form_for(@store_flavor) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :store_id %>
    <%= f.input :flavor_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
